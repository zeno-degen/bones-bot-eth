import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div className="flex flex-col items-center transition-all duration-300">
      <div className="w-full max-w-[1500px] flex md:mt-[172px] items-center justify-center relative flex-col"></div>
    </div>
  );
};

export default Home;
